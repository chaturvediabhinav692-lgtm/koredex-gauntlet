# koredex-gauntlet

Seeded-failure test harness for the Koredex CI classifier.
Each `seed/*` PR triggers exactly one failure class. See PR descriptions.

<!-- seed touch area below -->
seed touch: dependency
